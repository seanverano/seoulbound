//This module is used to export API Key and Image Endpoint to other JS files

// If the website doesnt display the data (ex: kdrama img and details)
// It means that I already deleted the API key below (both here and in the TMDB database so even if it's in the commit history it's no longer valid)
// Just send me a message through my socials so I can regenerate a new key for you

export const apiKey = "";
export const imgEndpoint = "https://image.tmdb.org/t/p/w500";
